---------
Responsible for creating the command line interface
---------

public void getOutput(String[] args)

CASE 1 - User types java TopSecret __ (no arg)
if args.length == 0 #meaning there is NO arguments
GET LIST OF ALL FILES #get list of all files, call from PartC
for (file : files)
    system.out.println(file)


CASE 2 - User types java TopSecret 1 or java TopSecret 01 or java TopSecret 01 somekey
if (args.length == 1 || args.length == 2) #1 number or 1 number + something
String index = args[0] # get the index of the number (so like 01 or 1)

try
    GET FILE CONTENTS # get the content of the file from the list based on the index
    catch (IllegalArgumentException e)
        return "Error: " + e.getMessage() + "\n" + usage();
    catch (Exception e)
        return "Error: " + e.getMessage() + "\n";
            }

CASE 3: too many args, ex. java TopSecret 1 2 3
return "Error: too many arguments"
}

GET LIST OF ALL FILES #get list of all files w/ numbers, call from PartC
GET FILE CONTENTS # get the content of the file from the list based on the index




